# 智慧房东 App 房源管理接口测试用例

> 项目：智慧房东 APP 房源管理接口测试
> API域名：api.zhihuifangdong.net
> 测试账号：18629604556，密码 a1234567
> 测试框架：Python + requests
> 文档来源：report-2026-05-11-zhihuifangdong_fy.md、report-2026-05-12-abab6.5s-chat.md、创建合同接口文档-2026-05-12.md

---

## 一、API 清单汇总

| # | 方法 | 路径 | 功能 |
|---|------|------|------|
| 1 | POST | `/core/app/houseManageExt/accurateSearch` | 精准搜索小区 |
| 2 | POST | `/core/cotenancyHouse/list` | 合租房列表查询 |
| 3 | GET | `/core/wholeHouse/getBlock` | 获取楼栋信息 |
| 4 | POST | `/core/houseAdd/wholeAdd` | 新增整租房源 |
| 5 | POST | `/core/houseAdd/cotenancyAdd` | 新增合租房源 |
| 6 | POST | `/core/houseAdd/cotenancyHouseEdit` | 编辑合租房间 |
| 7 | POST | `/core/app/house/checkHouseIndexMoreHb` | 整租房源详情确认（校验） |
| 8 | POST | `/core/checkHouseIndexPlus` | 合租房源详情确认（校验） |
| 9 | POST | `/findhouse/houseCommissionConfig/save` | 佣金配置保存 |
| 10 | GET | `/core/app/house/provinceChoose` | 省份列表获取 |
| 11 | GET | `/core/app/house/cityChooseA` | 城市列表获取 |
| 12 | GET | `/core/app/house/getCommunity` | 小区搜索 |
| 13 | GET | `/core/web/template/choose` | 公寓模板列表 |
| 14 | POST | `/core/web/apartment/addMore` | 批量添加公寓 |
| 15 | GET | `/core/app/houseManageExt/showLandLordCommunity` | 房东社区查询 |
| 16 | GET | `/core/manual/selectDevicePriceHandle` | 设备价格查询 |
| 17 | GET | `/core/app/house/getHouseIndex` | 整租房源详情 |
| 18 | GET | `/core/app/house/getCotenancyHouseIndex` | 合租房源详情 |

---

## 二、公共配置

```python
import requests

BASE_URL = "https://api.zhihuifangdong.net"
HEADERS = {
    "Content-Type": "application/json; charset=utf-8",
    "Authorization": "Bearer <JWT_TOKEN>",
    "User-Agent": "Mozilla/5.0 (Linux; Android 13; ...) Chrome/101.0.4951.74 Mobile Safari/537.36"
}

# 登录获取token（示例）
def login():
    resp = requests.post(
        f"{BASE_URL}/auth/login",
        json={"phone": "18629604556", "password": "a1234567"}
    )
    token = resp.json()["data"]["token"]
    HEADERS["Authorization"] = f"Bearer {token}"
    return token
```

---

## 三、整租流程测试用例

### TC-WH-001 精准搜索小区（整租模式）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-001 |
| **测试场景** | 整租流程 - 精准搜索小区 |
| **请求方式** | POST |
| **接口路径** | `/core/app/houseManageExt/accurateSearch` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `rentType=WHOLE`，`keyWord=中天` |
| **请求示例** | `rentType=WHOLE&keyWord=中天` |
| **预期结果** | `success=true`，`data.communityList` 返回匹配小区列表（含 communityId、communityName、districtName） |

### TC-WH-002 获取楼栋信息（整租）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-002 |
| **测试场景** | 整租流程 - 获取楼栋信息 |
| **请求方式** | GET |
| **接口路径** | `/core/wholeHouse/getBlock?type=WHOLE` |
| **关键参数** | `type=WHOLE` |
| **预期结果** | `success=true`，`data` 包含楼栋列表（blocked 楼幢号、dent 单元及楼层列表） |

### TC-WH-003 新增整租房源

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-003 |
| **测试场景** | 整租流程 - 添加整租房源 |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/wholeAdd` |
| **Content-Type** | `application/json; charset=utf-8` |
| **关键参数** | `provinceId=19`，`cityId=561`，`cityName=杭州市`，`communityId=362440`，`communityName=中天·官河锦庭`，`block=9幢`，`unit=1单元`，`name=102室`，`area=87`，`rent=320.00`，`roomNumber=2`，`livingRoomNumber=1`，`kitchenNumber=1`，`toiletNumber=1`，`payMethod=SEASON_PAY`，`pledgeCount=1`，`perMonth=3`，`commission=50`，`newPvCost=true` |
| **预期结果** | `success=true`，`data` 返回新增房源ID（Integer，如 2749273） |

### TC-WH-004 整租房源详情确认（校验）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-004 |
| **测试场景** | 整租流程 - 房源详情确认 |
| **请求方式** | POST |
| **接口路径** | `/core/app/house/checkHouseIndexMoreHb` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `id=2749273`（房源ID） |
| **预期结果** | `success=true`，`data` 包含房源完整信息（communityName=中天·官河锦庭，name=9幢1单元102室，area=87.0，rent=320.00，houseStatus.name=WAITING_RENT，roomNumber=2） |

### TC-WH-005 整租佣金配置保存

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-005 |
| **测试场景** | 整租流程 - 佣金配置 |
| **请求方式** | POST |
| **接口路径** | `/findhouse/houseCommissionConfig/save` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `houseId=2749273`，`houseRent=320.00`，`configs=[{"commissionType":2,"commissionValue":50,"startMonth":1,"endMonth":null}]` |
| **预期结果** | `success=true`，`message=保存成功` |

### TC-WH-006 整租佣金配置 - 固定金额类型

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-006 |
| **测试场景** | 整租流程 - 佣金配置（固定金额） |
| **请求方式** | POST |
| **接口路径** | `/findhouse/houseCommissionConfig/save` |
| **关键参数** | `houseId=2749273`，`houseRent=320.00`，`configs=[{"commissionType":1,"commissionValue":100,"startMonth":1,"endMonth":null}]`（commissionType=1为固定金额） |
| **预期结果** | `success=true`，`message=保存成功` |

### TC-WH-007 整租房源详情查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-WH-007 |
| **测试场景** | 整租流程 - 查询房源索引详情 |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/getHouseIndex?id=2749273` |
| **关键参数** | `id=2749273` |
| **预期结果** | `success=true`，`data` 包含 commission=50，rate=100，communityName=中天·官河锦庭，rentType=WHOLE |

---

## 四、合租流程测试用例

### TC-CH-001 精准搜索小区（合租模式）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-001 |
| **测试场景** | 合租流程 - 精准搜索小区 |
| **请求方式** | POST |
| **接口路径** | `/core/app/houseManageExt/accurateSearch` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `rentType=COTENANCY`，`keyWord=合嵣` |
| **请求示例** | `rentType=COTENANCY&keyWord=合嵣` |
| **预期结果** | `success=true`，`data.communityList` 返回合租匹配小区列表 |

### TC-CH-002 获取楼栋信息（合租）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-002 |
| **测试场景** | 合租流程 - 获取楼栋信息 |
| **请求方式** | GET |
| **接口路径** | `/core/wholeHouse/getBlock?type=COTENANCY` |
| **关键参数** | `type=COTENANCY` |
| **预期结果** | `success=true`，`data` 包含合租楼栋列表 |

### TC-CH-003 合租房列表查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-003 |
| **测试场景** | 合租流程 - 查看合租房列表 |
| **请求方式** | POST |
| **接口路径** | `/core/cotenancyHouse/list` |
| **Content-Type** | `application/json; charset=utf-8` |
| **关键参数** | `current=1`，`size=12`，`houseStatus=WAITING_RENT`，`communityId=395875`，`cityName=杭州市`，`communityName=合嵣悦府东区` |
| **预期结果** | `success=true`，`data.records` 包含合租房列表，每条含 simpleHouseList 和 sonHouse 子房间信息 |

### TC-CH-004 合租房列表 - 按状态筛选（已出租）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-004 |
| **测试场景** | 合租流程 - 按状态筛选房源（已出租） |
| **请求方式** | POST |
| **接口路径** | `/core/cotenancyHouse/list` |
| **关键参数** | `houseStatus=RENTED`，`communityId=395875` |
| **预期结果** | `success=true`，返回状态为 RENTED 的合租房源 |

### TC-CH-005 合租房列表 - 按状态筛选（签订中）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-005 |
| **测试场景** | 合租流程 - 按状态筛选房源（签订中） |
| **请求方式** | POST |
| **接口路径** | `/core/cotenancyHouse/list` |
| **关键参数** | `houseStatus=LEASE_SIGNING`，`communityId=395875` |
| **预期结果** | `success=true`，返回状态为 LEASE_SIGNING 的合租房源 |

### TC-CH-006 新增合租房源

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-006 |
| **测试场景** | 合租流程 - 添加合租房源单元 |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/cotenancyAdd` |
| **Content-Type** | `application/json; charset=utf-8` |
| **关键参数** | `provinceId=19`，`cityId=561`，`cityName=杭州市`，`communityId=395875`，`communityName=合嵣悦府东区`，`block=9幢`，`unit=1单元`，`name=101室`，`area=87`，`rent=301`，`newPvCost=true` |
| **预期结果** | `success=true`，`data` 返回合租父房源ID（如 2749274） |

### TC-CH-007 编辑合租房间（新增多个房间）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-007 |
| **测试场景** | 合租流程 - 编辑合租房间（新增多个房间） |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/cotenancyHouseEdit` |
| **Content-Type** | `application/json; charset=utf-8` |
| **关键参数** | `parentId=2749274`，`house=[{"houseId":0,"name":"1011","rent":"300","houseStatus":{"name":"WAITING_RENT"},"isEdit":true,"add":true},{"houseId":0,"name":"1012","rent":"400","houseStatus":{"name":"WAITING_RENT"},"isEdit":true,"add":true}]`，`roomNumber=2`，`livingRoomNumber=1`，`kitchenNumber=1`，`toiletNumber=1`，`payMethod=MONTH_PAY`，`pledgeCount=1`，`perMonth=1` |
| **预期结果** | `success=true`，`message=成功` |

### TC-CH-008 编辑合租房间（修改已有房间）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-008 |
| **测试场景** | 合租流程 - 编辑合租房间（修改已有房间租金） |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/cotenancyHouseEdit` |
| **关键参数** | `parentId=2749274`，`house=[{"houseId":2749278,"name":"1011","rent":"350","houseStatus":{"name":"WAITING_RENT"},"isEdit":true,"add":false}]`，`payMethod=MONTH_PAY`，`pledgeCount=1`，`perMonth=1` |
| **预期结果** | `success=true`，修改后房间租金更新为 350 |

### TC-CH-009 合租房源详情确认（校验）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-009 |
| **测试场景** | 合租流程 - 合租房源详情确认 |
| **请求方式** | POST |
| **接口路径** | `/core/checkHouseIndexPlus` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `id=2749274`（合租父房源ID） |
| **预期结果** | `success=true`，`data` 包含 sonHouse 数组（含所有子房间 ID、name、houseStatus），houseStatus.name=WAITING_RENT |

### TC-CH-010 合租佣金配置保存

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-010 |
| **测试场景** | 合租流程 - 佣金配置 |
| **请求方式** | POST |
| **接口路径** | `/findhouse/houseCommissionConfig/save` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **关键参数** | `houseId=2749274`，`houseRent=301.00`，`configs=[{"commissionType":2,"commissionValue":50,"startMonth":1,"endMonth":null}]` |
| **预期结果** | `success=true`，`message=保存成功` |

### TC-CH-011 合租房源详情查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-CH-011 |
| **测试场景** | 合租流程 - 查询合租房源索引详情 |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/getCotenancyHouseIndex?id=2749274` |
| **关键参数** | `id=2749274` |
| **预期结果** | `success=true`，`data` 包含合租房源完整信息及子房间列表 |

---

## 五、公寓流程测试用例

### TC-AP-001 省市区选择 - 获取省份列表

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-001 |
| **测试场景** | 公寓流程 - 省市区选择（省份） |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/provinceChoose` |
| **关键参数** | 无 |
| **预期结果** | `success=true`，返回省份列表（含 provinceId、provinceName），如 19=浙江省，1=上海市 |

### TC-AP-002 省市区选择 - 获取城市列表

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-002 |
| **测试场景** | 公寓流程 - 省市区选择（城市） |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/cityChooseA?id=19` |
| **关键参数** | `id=19`（省份ID） |
| **预期结果** | `success=true`，返回该省份下城市列表（如 561=杭州市） |

### TC-AP-003 搜索小区

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-003 |
| **测试场景** | 公寓流程 - 搜索小区 |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/getCommunity?name=新北路96弄&cityId=6758` |
| **关键参数** | `name=新北路96弄`，`cityId=6758` |
| **预期结果** | `success=true`，返回匹配小区列表（含 communityId、communityName） |

### TC-AP-004 公寓模板选择

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-004 |
| **测试场景** | 公寓流程 - 模板选择 |
| **请求方式** | GET |
| **接口路径** | `/core/web/template/choose` |
| **关键参数** | 无 |
| **预期结果** | `success=true`，`data` 返回模板列表（含 id、name、area、rent、roomDetail） |

### TC-AP-005 批量添加公寓

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-005 |
| **测试场景** | 公寓流程 - 批量添加公寓楼栋 |
| **请求方式** | POST |
| **接口路径** | `/core/web/apartment/addMore` |
| **Content-Type** | `application/json; charset=utf-8` |
| **关键参数** | `provinceId=1`，`provinceName=上海市`，`cityId=6758`，`cityName=奉贤区`，`communityId=3672`，`communityName=新北路96弄小区`，`block=1幢`，`unit=1单元`，`elevator=true`，`totalFloor=3`，`managementList=[{"floor":1,"select":true,"edit":false,"num":4,"sum":[{"value":1,"label":false},...],"list":[{"name":"A1001","templateId":10388,"templateName":"佣金30模版-副本"},...]}]` |
| **预期结果** | `success=true`，`message=成功` |

### TC-AP-006 批量添加公寓 - 多楼层配置

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-AP-006 |
| **测试场景** | 公寓流程 - 批量添加公寓（多楼层不同房间数） |
| **请求方式** | POST |
| **接口路径** | `/core/web/apartment/addMore` |
| **关键参数** | `totalFloor=3`，`managementList` 包含 floor=1(num=4)、floor=2(num=2)，每层使用不同模板 |
| **预期结果** | `success=true`，成功创建多楼层公寓 |

---

## 六、房东社区测试用例

### TC-LC-001 房东社区查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-LC-001 |
| **测试场景** | 房东社区 - 查询社区列表 |
| **请求方式** | GET |
| **接口路径** | `/core/app/houseManageExt/showLandLordCommunity` |
| **Query参数** | `current=1`，`size=10000`，`cityName=奉贤区`，`provinceId=1`，`provinceName=上海市` |
| **预期结果** | `success=true`，`data` 包含 total、records 数组（可能为空） |

### TC-LC-002 房东社区 - 杭州地区查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-LC-002 |
| **测试场景** | 房东社区 - 按杭州市查询 |
| **请求方式** | GET |
| **接口路径** | `/core/app/houseManageExt/showLandLordCommunity` |
| **Query参数** | `current=1`，`size=100`，`cityName=杭州市`，`provinceId=19`，`provinceName=浙江省` |
| **预期结果** | `success=true`，返回杭州地区房东社区数据 |

### TC-LC-003 房东社区 - 分页查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-LC-003 |
| **测试场景** | 房东社区 - 分页查询第二页 |
| **请求方式** | GET |
| **接口路径** | `/core/app/houseManageExt/showLandLordCommunity` |
| **Query参数** | `current=2`，`size=10`，`cityName=杭州市` |
| **预期结果** | `success=true`，`data.current=2`，`data.records` 返回对应页数据 |

---

## 七、设备价格测试用例

### TC-DV-001 设备价格查询

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-DV-001 |
| **测试场景** | 设备价格 - 查询水电煤计价配置 |
| **请求方式** | GET |
| **接口路径** | `/core/manual/selectDevicePriceHandle` |
| **关键参数** | 无 |
| **预期结果** | `success=true`，`data` 包含 `type=HUI_KUAN_DAY`，`initPrice=0.00` |

---

## 八、边界与异常测试用例

### TC-ERR-001 精准搜索 - 关键词为空

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-001 |
| **测试场景** | 异常 - 搜索关键词为空 |
| **请求方式** | POST |
| **接口路径** | `/core/app/houseManageExt/accurateSearch` |
| **关键参数** | `rentType=WHOLE`，`keyWord=`（空） |
| **预期结果** | `success=true`，但 `data.communityList=[]`（空数组） |

### TC-ERR-002 精准搜索 - 关键词无匹配结果

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-002 |
| **测试场景** | 异常 - 搜索关键词无匹配 |
| **请求方式** | POST |
| **接口路径** | `/core/app/houseManageExt/accurateSearch` |
| **关键参数** | `rentType=WHOLE`，`keyWord=xyz不存在的小区123` |
| **预期结果** | `success=true`，`data.communityList=[]` |

### TC-ERR-003 新增整租 - 必填参数缺失（缺少communityId）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-003 |
| **测试场景** | 异常 - 新增整租缺少必填参数 |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/wholeAdd` |
| **关键参数** | 缺少 `communityId` |
| **预期结果** | `success=false`，返回参数校验失败提示 |

### TC-ERR-004 新增整租 - 必填参数缺失（缺少rent）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-004 |
| **测试场景** | 异常 - 新增整租缺少租金参数 |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/wholeAdd` |
| **关键参数** | 缺少 `rent` |
| **预期结果** | `success=false`，返回参数校验失败提示 |

### TC-ERR-005 新增合租 - 必填参数缺失（缺少parentId）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-005 |
| **测试场景** | 异常 - 编辑合租房间缺少parentId |
| **请求方式** | POST |
| **接口路径** | `/core/houseAdd/cotenancyHouseEdit` |
| **关键参数** | 缺少 `parentId` |
| **预期结果** | `success=false`，返回参数校验失败提示 |

### TC-ERR-006 合租列表 - 无效房源状态

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-006 |
| **测试场景** | 异常 - 合租列表传入非法状态值 |
| **请求方式** | POST |
| **接口路径** | `/core/cotenancyHouse/list` |
| **关键参数** | `houseStatus=INVALID_STATUS` |
| **预期结果** | `success=true`，但返回空列表或忽略无效筛选条件 |

### TC-ERR-007 房源详情 - 不存在的房源ID

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-007 |
| **测试场景** | 异常 - 查询不存在的房源ID |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/getHouseIndex?id=999999999` |
| **关键参数** | `id=999999999` |
| **预期结果** | `success=false` 或 `data=null` |

### TC-ERR-008 佣金配置 - 佣金比例超限

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-008 |
| **测试场景** | 异常 - 佣金比例超出允许范围 |
| **请求方式** | POST |
| **接口路径** | `/findhouse/houseCommissionConfig/save` |
| **关键参数** | `houseId=2749273`，`commissionValue=200`（超过100%） |
| **预期结果** | `success=false` 或返回业务校验失败提示 |

### TC-ERR-009 城市列表 - 无效省份ID

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-009 |
| **测试场景** | 异常 - 查询无效省份ID对应的城市 |
| **请求方式** | GET |
| **接口路径** | `/core/app/house/cityChooseA?id=99999` |
| **关键参数** | `id=99999` |
| **预期结果** | `success=true`，但返回空列表 |

### TC-ERR-010 未登录访问（Token失效）

| 字段 | 内容 |
|------|------|
| **用例ID** | TC-ERR-010 |
| **测试场景** | 异常 - 使用无效/过期Token访问 |
| **请求方式** | POST |
| **接口路径** | `/core/app/houseManageExt/accurateSearch` |
| **关键参数** | `Authorization: Bearer invalid_token` |
| **预期结果** | `success=false`，返回鉴权失败（401或类似） |

---

## 九、Python 测试脚本示例

```python
import requests
import json

BASE_URL = "https://api.zhihuifangdong.net"

class TestZhihuifangdong:
    def __init__(self, token):
        self.headers = {
            "Content-Type": "application/json; charset=utf-8",
            "Authorization": f"Bearer {token}"
        }

    # ===== 整租流程 =====
    def test_accurate_search_whole(self):
        """精准搜索小区（整租）"""
        resp = requests.post(
            f"{BASE_URL}/core/app/houseManageExt/accurateSearch",
            data={"rentType": "WHOLE", "keyWord": "中天"},
            headers={"Authorization": self.headers["Authorization"],
                     "Content-Type": "application/x-www-form-urlencoded"}
        )
        assert resp.json()["success"] == True
        assert len(resp.json()["data"]["communityList"]) > 0

    def test_whole_add(self):
        """新增整租房源"""
        payload = {
            "provinceId": 19, "cityId": 561, "cityName": "杭州市",
            "communityId": 362440, "communityName": "中天·官河锦庭",
            "block": "9幢", "unit": "1单元", "name": "102室",
            "area": 87, "rent": "320.00", "roomNumber": 2,
            "livingRoomNumber": 1, "kitchenNumber": 1, "toiletNumber": 1,
            "payMethod": "SEASON_PAY", "pledgeCount": 1, "perMonth": 3,
            "commission": 50, "newPvCost": True
        }
        resp = requests.post(
            f"{BASE_URL}/core/houseAdd/wholeAdd",
            json=payload, headers=self.headers
        )
        assert resp.json()["success"] == True
        return resp.json()["data"]  # 返回房源ID

    def test_whole_check(self, house_id):
        """整租房源详情确认"""
        resp = requests.post(
            f"{BASE_URL}/core/app/house/checkHouseIndexMoreHb",
            data={"id": house_id},
            headers={"Authorization": self.headers["Authorization"],
                     "Content-Type": "application/x-www-form-urlencoded"}
        )
        assert resp.json()["success"] == True
        assert resp.json()["data"]["id"] == house_id

    def test_whole_commission(self, house_id):
        """整租佣金配置"""
        payload = {
            "houseId": house_id, "houseRent": "320.00",
            "configs": json.dumps([{
                "commissionType": 2, "commissionValue": 50,
                "startMonth": 1, "endMonth": None
            }])
        }
        resp = requests.post(
            f"{BASE_URL}/findhouse/houseCommissionConfig/save",
            data=payload,
            headers={"Authorization": self.headers["Authorization"],
                     "Content-Type": "application/x-www-form-urlencoded"}
        )
        assert resp.json()["success"] == True
        assert resp.json()["message"] == "保存成功"

    # ===== 合租流程 =====
    def test_cotenancy_add(self):
        """新增合租房源"""
        payload = {
            "provinceId": 19, "cityId": 561, "cityName": "杭州市",
            "communityId": 395875, "communityName": "合嵣悦府东区",
            "block": "9幢", "unit": "1单元", "name": "101室",
            "area": 87, "rent": 301, "newPvCost": True
        }
        resp = requests.post(
            f"{BASE_URL}/core/houseAdd/cotenancyAdd",
            json=payload, headers=self.headers
        )
        assert resp.json()["success"] == True
        return resp.json()["data"]

    def test_cotenancy_edit(self, parent_id):
        """编辑合租房间"""
        payload = {
            "parentId": parent_id,
            "house": [
                {"houseId": 0, "name": "1011", "rent": "300",
                 "houseStatus": {"name": "WAITING_RENT"},
                 "isEdit": True, "add": True,
                 "meterArr": [], "doorLockArr": [], "waterArr": [],
                 "meterAddForms": [], "waterAddForms": []},
                {"houseId": 0, "name": "1012", "rent": "400",
                 "houseStatus": {"name": "WAITING_RENT"},
                 "isEdit": True, "add": True,
                 "meterArr": [], "doorLockArr": [], "waterArr": [],
                 "meterAddForms": [], "waterAddForms": []}
            ],
            "roomNumber": 2, "livingRoomNumber": 1,
            "kitchenNumber": 1, "toiletNumber": 1,
            "payMethod": "MONTH_PAY", "pledgeCount": 1, "perMonth": 1
        }
        resp = requests.post(
            f"{BASE_URL}/core/houseAdd/cotenancyHouseEdit",
            json=payload, headers=self.headers
        )
        assert resp.json()["success"] == True

    # ===== 公寓流程 =====
    def test_province_choose(self):
        """获取省份列表"""
        resp = requests.get(
            f"{BASE_URL}/core/app/house/provinceChoose",
            headers=self.headers
        )
        assert resp.json()["success"] == True
        assert len(resp.json()["data"]) > 0

    def test_apartment_add(self):
        """批量添加公寓"""
        payload = {
            "provinceId": "1", "provinceName": "上海市",
            "cityId": "6758", "cityName": "奉贤区",
            "communityId": "3672", "communityName": "新北路96弄小区",
            "block": "1幢", "unit": "1单元",
            "elevator": True, "totalFloor": 3,
            "managementList": [{
                "floor": 1, "select": True, "edit": False, "num": 4,
                "sum": [{"value": 1, "label": False},
                       {"value": 2, "label": False}],
                "list": [
                    {"name": "A1001", "templateId": 10388,
                     "templateName": "佣金30模版-副本"}
                ]
            }]
        }
        resp = requests.post(
            f"{BASE_URL}/core/web/apartment/addMore",
            json=payload, headers=self.headers
        )
        assert resp.json()["success"] == True

    # ===== 房东社区 =====
    def test_landlord_community(self):
        """房东社区查询"""
        resp = requests.get(
            f"{BASE_URL}/core/app/houseManageExt/showLandLordCommunity",
            params={"current": 1, "size": 10000,
                    "cityName": "奉贤区", "provinceId": 1,
                    "provinceName": "上海市"},
            headers=self.headers
        )
        assert resp.json()["success"] == True

    # ===== 设备价格 =====
    def test_device_price(self):
        """设备价格查询"""
        resp = requests.get(
            f"{BASE_URL}/core/manual/selectDevicePriceHandle",
            headers=self.headers
        )
        assert resp.json()["success"] == True
        assert "type" in resp.json()["data"]
```

---

## 十、枚举值参考

| 类型 | 枚举值 | 说明 |
|------|--------|------|
| 房源类型 rentType | `WHOLE` | 整租 |
| 房源类型 rentType | `COTENANCY` | 合租 |
| 房源状态 houseStatus.name | `WAITING_RENT` | 未出租 |
| 房源状态 houseStatus.name | `RENTED` | 已出租 |
| 房源状态 houseStatus.name | `LEASE_SIGNING` | 出租签订中 |
| 房源状态 houseStatus.name | `NOT_RENT` | 非租 |
| 房源状态 houseStatus.name | `PRE_LINE` | 预上线 |
| 支付方式 payMethod | `MONTH_PAY` | 月付 |
| 支付方式 payMethod | `SEASON_PAY` | 季付 |
| 支付方式 payMethod | `HALF_YEAR_PAY` | 半年付 |
| 支付方式 payMethod | `YEAR_PAY` | 年付 |
| 上下架状态 onShelf | `UP_SHELF` | 上架 |
| 上下架状态 onShelf | `DOWN_SHELF` | 下架 |
| 佣金类型 commissionType | `1` | 固定金额 |
| 佣金类型 commissionType | `2` | 固定比例（百分比） |

---

## 十一、用例汇总统计

| 流程 | 用例数量 | 用例ID |
|------|----------|--------|
| 整租流程 | 7 | TC-WH-001 ~ TC-WH-007 |
| 合租流程 | 11 | TC-CH-001 ~ TC-CH-011 |
| 公寓流程 | 6 | TC-AP-001 ~ TC-AP-006 |
| 房东社区 | 3 | TC-LC-001 ~ TC-LC-003 |
| 设备价格 | 1 | TC-DV-001 |
| 边界与异常 | 10 | TC-ERR-001 ~ TC-ERR-010 |
| **合计** | **38** | |

---

*文档生成时间：2026-05-21*
*接口来源：report-2026-05-11-zhihuifangdong_fy.md / report-2026-05-12-abab6.5s-chat.md / 创建合同接口文档-2026-05-12.md*
